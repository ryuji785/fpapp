spring:
  config:
    import: optional:file:../.env[.properties]
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/fpapp}
    username: ${DB_USERNAME:fpapp}
    password: ${DB_PASSWORD:fpapp_dev}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    url: ${DB_URL:jdbc:postgresql://localhost:5432/fpapp}
    user: ${DB_USERNAME:fpapp}
    password: ${DB_PASSWORD:fpapp_dev}
    clean-disabled: false
    locations: classpath:db/migration
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
  mvc:
    format:
      date: yyyy-MM-dd
  web:
    cors:
      allowed-origins: http://localhost:5173
  mybatis:
    configuration:
      map-underscore-to-camel-case: true
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: app.fp.domain
server:
  servlet:
    context-path: ""
